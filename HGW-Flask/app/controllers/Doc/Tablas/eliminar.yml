summary: Eliminar un registro de una tabla automapeada
description: |
  Elimina el registro según su ID y registra la acción en la tabla `registro_logs`.

tags:
  - 👁️‍🗨️ Tablas

consumes:
  - application/json

parameters:
  - in: body
    name: datos
    schema:
      type: object
      required:
        - table
        - id
        - log
      properties:
        table:
          type: string
        id:
          type: integer
        log:
          type: object
          properties:
            id:
              type: integer
            accion:
              type: string

responses:
  200:
    description: Registro eliminado exitosamente
    schema:
      type: object
      properties:
        respuesta:
          type: string
          example: "Se ha eliminado el registro"

  404:
    description: Registro no encontrado

  200:
    description: Respuesta OPTIONS válida
