summary: Consulta paginada de una tabla
description: |
  Devuelve filas y columnas de la tabla solicitada.  
  Incluye paginación, búsqueda general y serialización con lookup de FKs.

tags:
  - 👁️‍🗨️ Tablas

consumes:
  - application/json

parameters:
  - in: body
    name: req
    required: true
    schema:
      type: object
      properties:
        table:
          type: string
          required: true
        page:
          type: integer
          example: 0
        limit:
          type: integer
          example: 10
        busqueda:
          type: string
          example: "juan"

responses:
  200:
    description: Información completa de la tabla
    schema:
      type: object
      properties:
        filas:
          type: array
          items:
            type: object
        columnas:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              field:
                type: string
        total:
          type: integer
          example: 25

  400:
    description: Tabla no encontrada
    schema:
      type: object
      properties:
        error:
          type: string

  200:
    description: Respuesta OPTIONS válida
