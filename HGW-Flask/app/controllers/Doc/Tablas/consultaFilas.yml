summary: Obtener un registro específico de una tabla
description: |
  Devuelve un solo registro por ID, serializado con lookup de llaves foráneas.

tags:
  - 👁️‍🗨️ Tablas

consumes:
  - application/json

parameters:
  - in: body
    name: datos
    schema:
      type: object
      required:
        - table
        - id
      properties:
        table:
          type: string
        id:
          type: integer

responses:
  200:
    description: Registro encontrado
    schema:
      type: object
      additionalProperties: true

  404:
    description: Registro no encontrado
    schema:
      type: object
      properties:
        error:
          type: string

  200:
    description: Respuesta OPTIONS válida
