summary: Obtener la membresía activa de un usuario
description: |
  Devuelve la membresía actual del usuario, junto con:
    - Precio de la membresía
    - BV requeridos para ese nivel
    - BV acumulados del usuario
    - Próxima membresía a la que puede ascender (si existe)

  Si el usuario no existe o no tiene una membresía registrada, se retornará un error.
tags:
  - 🏅 Membresía
parameters:
  - name: id
    in: query
    required: true
    type: integer
    description: ID del usuario
    example: 15
responses:
  200:
    description: Membresía obtenida exitosamente
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: true
        membresia:
          type: object
          properties:
            id_membresia:
              type: integer
              example: 3
            nombre_membresia:
              type: string
              example: "Oro"
            precio_membresia:
              type: number
              example: 39.99
            bv_requeridos:
              type: integer
              example: 1200
            bv_acumulados:
              type: integer
              example: 860
            proxima_membresia:
              type: string
              nullable: true
              example: "Platino"
    examples:
      application/json:
        success: true
        membresia:
          id_membresia: 3
          nombre_membresia: "Oro"
          precio_membresia: 39.99
          bv_requeridos: 1200
          bv_acumulados: 860
          proxima_membresia: "Platino"

  400:
    description: ID no proporcionado
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: "ID de usuario no proporcionado"

  404:
    description: Membresía no encontrada para el usuario
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: "Membresía no encontrada"

  500:
    description: Error interno del servidor
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: "Error al obtener membresía: detalle"
