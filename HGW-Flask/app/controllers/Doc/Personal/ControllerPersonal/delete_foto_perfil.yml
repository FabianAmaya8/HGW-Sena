summary: Eliminar foto de perfil del usuario
description: |
  Elimina físicamente la foto de perfil del servidor y actualiza la BD colocando url_foto_perfil en NULL.
tags:
  - 👤 Usuario
parameters:
  - name: id
    in: query
    required: true
    type: integer
    description: ID del usuario
responses:
  200:
    description: Foto eliminada correctamente
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: true
        message:
          type: string
          example: "Foto eliminada"
  400:
    description: ID no proporcionado
  404:
    description: No hay foto para borrar
  500:
    description: Error interno
