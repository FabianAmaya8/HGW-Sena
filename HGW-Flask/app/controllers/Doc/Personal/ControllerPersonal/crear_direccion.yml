summary: Crear una nueva dirección para el usuario
description: |
  Valida ciudad y país antes de registrar.  
  Inserta la dirección asociada al usuario y retorna el ID creado.
tags:
  - 🚩 Direcciones
consumes:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    description: Datos de la dirección
    schema:
      type: object
      required:
        - id_usuario
        - lugar_entrega
        - direccion
        - ciudad
        - pais
        - codigo_postal
      properties:
        id_usuario:
          type: integer
          example: 8
        lugar_entrega:
          type: string
          example: "Casa"
        direccion:
          type: string
          example: "Calle 12 #45-66"
        ciudad:
          type: string
          example: "Bogotá"
        pais:
          type: string
          example: "Colombia"
        codigo_postal:
          type: string
          example: "110111"
responses:
  201:
    description: Dirección creada exitosamente
    schema:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
        id_direccion:
          type: integer
          example: 55
  400:
    description: Datos incompletos o ciudad/pais inválidos
  500:
    description: Error interno
