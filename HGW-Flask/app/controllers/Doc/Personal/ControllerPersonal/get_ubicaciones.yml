summary: Obtener países y sus ciudades
description: |
  Retorna una estructura donde cada país contiene una lista de nombres de ciudades disponibles.
tags:
  - 👤 Usuario
responses:
  200:
    description: Ubicaciones obtenidas correctamente
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: true
        ubicaciones:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
    examples:
      application/json:
        success: true
        ubicaciones:
          Colombia:
            - Bogotá
            - Medellín
            - Cali
          México:
            - CDMX
            - Guadalajara
  500:
    description: Error interno del servidor
    schema:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
          example: "Error interno del servidor"
