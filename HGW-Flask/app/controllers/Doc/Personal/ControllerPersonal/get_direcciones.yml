summary: Obtener todas las direcciones del usuario
description: |
  Retorna todas las direcciones registradas del usuario, incluyendo país y ciudad.
tags:
  - 👤 Usuario
parameters:
  - name: id
    in: query
    required: true
    type: integer
    description: ID del usuario
responses:
  200:
    description: Direcciones obtenidas correctamente
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: true
        direcciones:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
                example: 12
              direccion:
                type: string
                example: "Calle 12 #45-67"
              codigo_postal:
                type: string
                example: "110111"
              lugar_entrega:
                type: string
                example: "Casa"
              ciudad:
                type: string
                example: "Bogotá"
              pais:
                type: string
                example: "Colombia"
  400:
    description: ID no proporcionado
  500:
    description: Error interno del servidor
