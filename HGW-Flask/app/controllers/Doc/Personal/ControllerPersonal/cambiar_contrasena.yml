summary: Cambiar contraseña del usuario
description: |
  Valida la contraseña actual.  
  Si es correcta, la reemplaza por la nueva hashada con bcrypt.
tags:
  - 👤 Usuario
consumes:
  - application/json
parameters:
  - in: body
    name: payload
    required: true
    schema:
      type: object
      required:
        - id_usuario
        - actual
        - nueva
      properties:
        id_usuario:
          type: integer
          example: 7
        actual:
          type: string
          example: "MiClaveActual123"
        nueva:
          type: string
          example: "MiNuevaClave987"
responses:
  200:
    description: Contraseña actualizada correctamente
    schema:
      type: object
      properties:
        success:
          type: boolean
          example: true
        message:
          type: string
          example: "Contraseña actualizada correctamente"
  400:
    description: Datos incompletos
  401:
    description: Contraseña actual incorrecta
  404:
    description: Usuario no encontrado
  500:
    description: Error interno
