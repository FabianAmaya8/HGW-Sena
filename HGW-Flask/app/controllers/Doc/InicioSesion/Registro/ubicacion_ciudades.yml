summary: Listar ciudades
description: |
  Recupera todas las ciudades registradas.
  Si se proporciona el parámetro `paisId`, solo devuelve las ciudades pertenecientes a ese país.
tags:
  - 🚩 Direcciones
parameters:
  - in: query
    name: paisId
    type: integer
    required: false
    description: Filtrar ciudades por ID del país
    example: 1
responses:
  200:
    description: Lista de ciudades obtenida exitosamente
    schema:
      type: array
      items:
        type: object
        properties:
          id_ubicacion:
            type: integer
            example: 10
          nombre:
            type: string
            example: "Bogotá"
          ubicacion_padre:
            type: integer
            example: 1
    examples:
      application/json:
        - id_ubicacion: 10
          nombre: "Bogotá"
          ubicacion_padre: 1
        - id_ubicacion: 11
          nombre: "Medellín"
          ubicacion_padre: 1
  500:
    description: Error interno al obtener ciudades
    schema:
      type: object
      properties:
        error:
          type: string
          example: "Error interno al obtener ciudades"
